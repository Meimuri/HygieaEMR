const {
    validUser,
    validUsername,
    invalidUsers,
    nonExistentUserId,
} = require("./user_data");

module.exports = {
    validUser,
    validUsername,
    invalidUsers,
    nonExistentUserId,
};
